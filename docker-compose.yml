services:
  # PostgreSQL Databases
  auth_db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: auth_user
      POSTGRES_PASSWORD: auth_password
      POSTGRES_DB: auth_db
    ports:
      - "5432:5432"
    volumes:
      - auth_db_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U auth_user -d auth_db"]
      interval: 10s
      timeout: 5s
      retries: 5

  mail_db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: mail_user
      POSTGRES_PASSWORD: mail_password
      POSTGRES_DB: mail_db
    ports:
      - "5433:5432"
    volumes:
      - mail_db_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U mail_user -d mail_db"]
      interval: 10s
      timeout: 5s
      retries: 5

  # RabbitMQ Message Queue
  rabbitmq:
    image: rabbitmq:3.12-management-alpine
    environment:
      RABBITMQ_DEFAULT_USER: guest
      RABBITMQ_DEFAULT_PASS: guest
    ports:
      - "5672:5672"
      - "15672:15672"
    volumes:
      - rabbitmq_data:/var/lib/rabbitmq
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Auth Service
  auth_service:
    build: ./services/auth-service
    ports:
      - "8001:8001"
    environment:
      AUTH_DB_URL: postgresql://auth_user:auth_password@auth_db:5432/auth_db
      JWT_SECRET: your-secret-key-change-in-production
      RABBITMQ_URL: amqp://guest:guest@rabbitmq:5672/
    depends_on:
      auth_db:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy

  # Mail Service
  mail_service:
    build: ./services/mail-service
    ports:
      - "8002:8002"
    environment:
      DATABASE_URL: postgresql+psycopg2://mail_user:mail_password@mail_db:5432/mail_db
      RABBITMQ_URL: amqp://guest:guest@rabbitmq:5672/
      MAIL_SERVICE_PORT: 8002
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8002/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    depends_on:
      mail_db:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy

  # API Gateway
  api_gateway:
    build: ./services/api-gateway
    environment:
      AUTH_SERVICE_URL: http://auth_service:8001 # internal port inside auth service
      MAIL_SERVICE_URL: http://mail_service:8002 # internal port inside mail service
      SECRET_KEY: your-secret-key-change-in-production
    ports:
      - "8000:8000"
    depends_on:
      - auth_service
      - mail_service

volumes:
  auth_db_data:
  mail_db_data:
  rabbitmq_data:

networks:
  default:
    name: micromail_network
